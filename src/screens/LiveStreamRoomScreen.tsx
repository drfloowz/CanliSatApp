import React from 'react';
import { View, Text } from 'react-native';

export const LiveStreamRoomScreen = () => {
  return (
    <View className="flex-1 items-center justify-center bg-white dark:bg-gray-900">
      <Text className="text-black dark:text-white text-xl font-bold">Live Stream Room (Full Screen)</Text>
    </View>
  );
};
